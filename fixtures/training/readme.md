# Governed Training Fixtures

This dataset and config are used to validate Volume 5 SLAs:
- `Conf_min` must be satisfied.
- No safety regression is allowed.
- Drift `D` must stay `<= τ_warn`.
- Promotion only allowed when all objectives are met.
